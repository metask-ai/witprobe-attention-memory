# 冻结的数字与口径清单(CANON)

**本文件由 `tools/make_canon.py` 从 `experiments/out/*.json` 自动生成,请勿手改。**

`tests/test_paper_claims.py` 用它同时核查中文稿、`papers/p1-kv-certificates/arxiv/main.tex`、
`papers/p1-kv-certificates/abstract_en.md` —— 任何一份文档漏写或写错其中一个数字都会 FAIL。
这是为了解决『代码比论文成熟得快、多份文档相互矛盾』的问题。

## 理论

| 量 | 值 | 口径 |
|---|---|---|
| 反例倍数 | **7.8** | 两 token 反例:真实 TV 是错误式给出的『界』的 7.8 倍 |
| 错误式通过的检查组数 | **32,256** | 均值口径检查数;逐 query 检查才暴露问题 |
| Lean 主定理数 | **4** | Mathlib 层 L1–L4,公理仅 propext/Classical.choice/Quot.sound;由 lean_extract 抽取 |
| 机器检查定理总数 | **20** | Mathlib 层 11 + 零依赖层 9;由 formal/Export.lean 用 Lean 自己的 pretty-printer 与 collectAxioms 导出,陈述编译进 papers/p1-kv-certificates/THEOREMS.md 与 papers/p1-kv-certificates/arxiv/theorems.tex |
| 公理并集 | **Classical.choice、Quot.sound、propext** | collectAxioms 机器给出,无 sorryAx |
## RULER门控CI

| 量 | 值 | 口径 |
|---|---|---|
| fp8 门控配对差 | **+0.3 [+0.0,+0.8]** | 逐样本配对 vs 满血,150 样本,bootstrap 10⁴;risk-ranked τ=5 |
| int8/kivi 门控配对差 | **0.0** | 两配置与满血在每个样本上判分完全一致(配对差恒 0) |
| fp8 裸跑配对差 | **-56.6 [-64.1,-49.1]** | 崩塌统计显著 |
| kivi 裸跑配对差 | **-8.1 [-13.8,-2.6]** | 静默尾部从轶事升级为统计显著;复跑均值 71.3(原切片 73.7,run-to-run 漂移) |
## 128k覆盖率

| 量 | 值 | 口径 |
|---|---|---|
| 8k 区间 | **63.7–64.5%** | Qwen2.5-1.5B,两域拼接流,P17 同配置,δ=10⁻²,KV-head any-of-G |
| 32k 区间 | **57.3–60.0%** | 同上 |
| 128k 区间 | **26.6–41.3%** | 同上,YaRN factor=4(厂商官方长上下文配置);tanh 参考塌到 ≤0.2% |
| 128k tanh 上限 | **0.2%** | 128k 下确定性 tanh 界的最好域 |
| 违约 | **0** | 三档 × 两域全部零违约(soundness 第四条独立证据链) |
## 双侧界

| 量 | 值 | 口径 |
|---|---|---|
| 有效档中位反松 | **1.7×** | rtn-8bit(有效档)双侧聚合形/旧 e-form 的中位比;双侧形在小 c 区二阶劣化 |
| 有效档胜出比例 | **1.6%** | rtn-8bit,双侧形更紧的单元占比 |
| coverage 变化 | **0.445→0.446** | τ=0.2,min(1,旧,双侧,逐token min) 相对旧界;实质无变化 |
| min形数值soundness | **0/26,880** | 全部六量化器单元 meter ≥ 真 TV 的违约计数 |
| 饱和档最大缩减 | **26,433×** | rtn-2bit 中位缩减(但仍远高于 1,meter 恒饱和,无操作点变化) |
## δ曲线

| 量 | 值 | 口径 |
|---|---|---|
| subG 覆盖率区间 | **54.4–81.0%** | 三模型族(Qwen2.5-7B/Mistral-7B/Yi-1.5-6B)×三域,固定 δ=10⁻²,域内均值,KV-head any-of-G |
| tanh 覆盖率区间 | **11.8–52.0%** | 同上,确定性 tanh 界 |
| 换页率相对下降 | **28.7–77.0%** | δ=10⁻² 下相对 tanh |
| δ 收紧 500× 最大损失 | **3.2pp** | 5×10⁻²→10⁻⁴,六个模型×域组合的最大值 |
| 模型族数 | **3** | Qwen2.5-7B(8k)/Mistral-7B(8k)/Yi-1.5-6B(4k,受其位置上限) |
## RULER-4k

| 量 | 值 | 口径 |
|---|---|---|
| WitCert 均分 | **94.42** | 13 任务均值,Qwen2.5-7B,1300 样本,字节预算 46.5% |
| 满血均分 | **94.30** | no_press |
| EA 均分 | **93.88** | expected_attention,其 compression_ratio=0.401 = 丢 40.1% token |
| 差值 | **+0.53** | 配对 bootstrap p≈0.12,**不显著**;两列预算量纲不同 |
| 字节节省 | **46.5%** | K 145.1B + V 129B = 274.1B vs fp16 512B |
## LongBench-E

| 量 | 值 | 口径 |
|---|---|---|
| 长度桶数 | **15** | 5 任务 × 3 桶;lcc_e 三配置全部运行失败未计入 |
| WitCert 跌幅>1pp 桶数 | **WitCert 1/15** |  |
| EA 跌幅>1pp 桶数 | **EA 4/15** |  |
| 2wikimqa 4-8k WitCert | **−3.23** | 唯一实质下降 |
| 2wikimqa 4-8k EA | **−7.79** | 同桶,我们的 2.4 倍 |
| 对我方不利处 | **+4.36** | EA 在 hotpotqa_e 8k+ 明显更好(如实列出) |
## 质量

| 量 | 值 | 口径 |
|---|---|---|
| 抖动增益(质量中性) | **1.01×** | 真实 V,Qwen2.5-7B/8k;抖动本身质量中性 |
| 离群旁路增益 | **1.38×** | 同上 |
## kernel

| 量 | 值 | 口径 |
|---|---|---|
| 四联报覆盖率 | **79.2%** | S=32768,nq=24,τ=0.2,合成 K |
| 四联报时延 | **8.627** | ms |
| 四联报加速比 | **5.41×** | **kernel 级;不可作为部署代价引用**,端到端见 serving 表 |
| 四联报误差 | **9.0e-3** |  |
| 真实显存节省 | **43.3%** | torch.cuda.memory_allocated 实测,8k/32k/128k 一致 |
| 回退后误差 | **3.32e-4** | 回退前 9.18e-3 |
| 有效位宽 | **10.96** | 门控后,净省 31.5% |
| 块级换页流量降低 | **128×** | 中位 1/128 块即回落阈值 |
| 长 CoT KV 头覆盖率 | **0.800** | R1-Distill-7B 真实生成,n=4;与常规长文 0.792 持平 |
| 随机舍入分支覆盖率 | **0.000** | 所有离群预算下恒为 0 ⟹ 被数据否决 |
| m=4 覆盖率(实机) | **0.792** | S=32768,τ=0.2,合成 K |
| m=8 覆盖率(实机) | **0.958** | S=32768,τ=0.2,合成 K |
| m=16 覆盖率(实机) | **1.000** | S=32768,τ=0.2,合成 K |
## 端到端解码

| 量 | 值 | 口径 |
|---|---|---|
| m=16 覆盖率 | **1.000** | HF Cache,28 层,真实 query |
| m=16 KL | **4.16e-3** | teacher-forcing |
| m=16 top-5 重叠 | **0.981** |  |
| m=16 最大证书 | **0.086** | 距 τ=0.2 有 2.3× 余量 |
## scale粒度

| 量 | 值 | 口径 |
|---|---|---|
| 论文口径误差 | **2.449e-2** | 逐块逐通道,分页不兼容 |
| G=32 相对论文 | **0.87×** | 逐 token 逐 32 通道组,分页兼容且更准 |
## 离群旁路

| 量 | 值 | 口径 |
|---|---|---|
| Qwen 通道稳定性 Jaccard | **0.940** | 前半/后半 token 各选 top-8 对 |
| Qwen m=0 覆盖率 | **0.525** | S=4096,τ=0.2,逐 token 组 scale |
| Qwen m=4 覆盖率 | **0.843** | 已排除 amax |
| Qwen m=16 覆盖率 | **0.980** |  |
| Yi 通道稳定性 Jaccard | **0.880** | 前半/后半 token 各选 top-8 对 |
| Yi m=0 覆盖率 | **0.907** | S=4096,τ=0.2,逐 token 组 scale |
| Yi m=4 覆盖率 | **1.000** | 已排除 amax |
| Yi m=16 覆盖率 | **1.000** |  |
| 只旁路不排除 amax 的降幅 | **8%** | 证书上界只降 8% ⟹ 必须同时排除 |
| 旁路+排除 amax 的降幅 | **32×** | 合成对照:证书最大 2.378 → 0.0731 |
## RULER门控

| 量 | 值 | 口径 |
|---|---|---|
| 满血 avg | **79.3** | 6 难任务×25样本=150条,7B,SGLang观测台 |
| fp8 裸跑 avg | **22.8** | 灾难级;零分样本 114/150 |
| fp8 门控 avg | **79.7** | τ_gate=5;零分回到 19(=满血);PCIe 26.1 GiB 一次性 |
| kivi 裸跑 avg | **73.7** | 尾部损伤:零分 28 vs 满血 19(9 个静默失败) |
| kivi 门控 avg | **79.3** | 9 个静默失败全部消除;PCIe 30.5 GiB |
| int8 门控 PCIe | **7.7 GiB** | 安全方案流量比 fp8 少 3.4×(增量持久修复;选择性在规模上成立) |
| 增量修复流量降幅 | **41×** | 整请求重换页 bug 修复:fp8_gate02 1267→30.9 GiB |
## 机理

| 量 | 值 | 口径 |
|---|---|---|
| 单层污染有损层数 | **0/28** | kivi2 逐层单独污染,needle 1.5B;全部无损 ⟹ 无关键层,失败纯来自跨层累积 |
| 全层污染 acc | **0.958** | 对照:单层全部 1.0 |
| kvquant4 覆盖率 | **0.00066** | 第二个真实方法复刻:needle 满分但逐步认证不可得,与 KIVI 互证 |
## 重复性

| 量 | 值 | 口径 |
|---|---|---|
| 满血三切片均值 | **83.3** | 79.3/87.2/83.4;所有方向性结论三切片复现 |
| fp8裸跑三切片均值 | **26.4** | 22.8/27.8/28.7 —— 灾难在每个切片复现 |
| 门控距满血差距 | **0.4** | fp8/kivi 门控三切片均值与满血差 ≤0.4;kivi 额外零分(+9~+12)全消 |
## 界形对照

| 量 | 值 | 口径 |
|---|---|---|
| det门 fp8 PCIe | **12.1 GiB** | τ=0.99,同见证输入;e-form 同 τ 为 30.8 GiB ⟹ Tier-A 档 det-tanh 门便宜 2.5× |
| det门 int8 PCIe | **5.3 GiB** | e-form 同 τ 27.3 GiB(5.2×);两界形质量均恢复(79.3–80.1)。受控抖动器档 e-form 反超 |
## serving

| 量 | 值 | 口径 |
|---|---|---|
| 1.5B fp16 容量 | **650,368** | page_size=16,mem_frac 0.88,batch16×64tok |
| 1.5B WitCert 容量 | **1,224,240** | 1.88× |
| 1.5B 容量倍率 | **1.88×** | 同显存预算 |
| 1.5B 吞吐倍率 | **0.75×** | 3 次取中位数 |
| 1.5B 覆盖率(无旁路) | **0.812** | 生产遥测,抽样 stride 32 |
| 1.5B 旁路容量 | **1,095,360** | 1.68× |
| 1.5B 旁路覆盖率 | **0.940** | m=4 固定通道集 |
| 1.5B 证书边际(带旁路) | **2.4%** |  |
| 7B fp16 容量 | **114,128** | page_size=16,mem_frac 0.88,batch16×64tok |
| 7B WitCert 容量 | **214,832** | 1.88× |
| 7B 容量倍率 | **1.88×** | 同显存预算 |
| 7B 吞吐倍率 | **0.82×** | 3 次取中位数 |
| 7B 覆盖率(无旁路) | **0.753** | 生产遥测,抽样 stride 32 |
| 7B 旁路容量 | **192,224** | 1.68× |
| 7B 旁路覆盖率 | **0.946** | m=4 固定通道集 |
| 7B 证书边际(带旁路) | **11.8%** |  |
| 证书边际(1.5B 无旁路) | **6.4%** | 短序列 decode;长序列微基准是 +0.35% |
| 证书边际(7B 无旁路) | **16.2%** | 同上 |
| kernel 级证书成本 | **+0.35%** | S=4096 微基准,量化存储路径 |
| fp16 路径证书成本 | **+11.9%** | SGLang decode kernel,存储未量化时 |
| 写路径 Triton 加速 | **82×** | torch 版在 decode 循环里会主导时延(63.7→575.9 tok/s) |
| 补丁行数 | **500 / 155 / 327** | decode / extend / serving 接线,三份可提 PR;行数由 freeze_integration_stats 现算冻结进产物,不手抄 |
## H200战役

| 量 | 值 | 口径 |
|---|---|---|
| 自适应违约(自由解码) | **0/1000** | Qwen2.5-7B,50 prompts×20 seeds,prefill 4096,decode 64,δ_req=1e-2 |
| 自适应监测 cell 总数 | **100,352,000** | 每步×每层×每 q-head,free+forced 双模式;数值容差 1e-9,零容差残余全部 |边际|≤1.8e-13 |
| 自适应轨迹分歧率 | **62.9%** | free 组与 fp16 基线分歧,中位第 22 步;rule-of-three 上界 0.3% |
| 最坏 TV/界比 | **0.536** | 全部 informative cell 上取最大 |
| RC 对照 Δppl | **+0.0059** | Llama-3.1-8B,ctx8192×8 配对窗;WitCert 同框 +0.0123 |
| RC 页入 | **264 MB/请求** | RC 快路径常态促升页入(RULER-4096);WitCert 认证态为 0,且无 CPU 常驻副本 |
| packed 门控闭环质量 | **78.83** | RULER 6 难任务×25,τ=0.2;= packed 无门控 78.83,fp16 78.67 |
| 门控逐请求页入 P50/P95 | **2.81 / 3.58 GiB** | 串行差分口径,150 请求;packed 修复非持久化固有成本 |
| 容量比跨规模/并行不变 | **1.882×** | 7B/14B/32B(单卡)、70B(tp4)、7B(tp2/tp4) 全部 1.8824×;结构决定 |
| 70B tp4 容量 | **2,183,776** | fp16 1,160,128;吞吐比 0.79× |
| 原生 128k 覆盖率(needle) | **77.0 / 77.1 / 77.0%** | Llama-3.1-8B 原生窗口(无 YaRN)@8k/32k/128k,零违约——随长度平坦;1.5B+YaRN 档的下滑为 YaRN 外推特有 |
| 前沿最不利工况点 | **0.43×** | c=512 短请求(1024in/256out):witcert 6206 vs fp16 14324 tok/s;单流 0.78×,tp2 反超 |
| 长上下文代价比例平稳 | **1.84×→1.89×** | Llama-8B witcert TTFT 中位比 @8k→32k(TPOT 1.47×→1.49×),不随 S 恶化;qwen7b cert-on 30k TPOT 异常已列工程项 |
| 72B 覆盖率 | **25.5–29.8%** | jkv@δ=1e-2 三域,零违约;显著低于 7–32B(54–81%),削弱性结果如实报告(与 Yi 同型) |
## R8平台

| 量 | 值 | 口径 |
|---|---|---|
| latent 尺子 cells | **4,032** | V2-Lite S=4096,9 文档×3 域,7 层采样×头步 2(MLA latent 数学验证) |
| latent int8 双 Tier 违约 | **0/4,032** | Tier A 零违约;Tier A soundness 由此承担,在线观测只给量级与紧度 |
| latent dither Tier B 覆盖率 | **98.9%** | τ=0.2;τ=0.5 时 100% |
| GLM 生产 fp8 相对残差 | **0.93%** | GLM-5.2 W4AFP8 生产栈在线观测,78 层×8 rank,相对 latent 范数 |
| GLM latent 层间异质 | **493×** | latent 范数最大/最小之比,异构策略论据 |
| 坏块哨兵假阳性率 | **0%** | 144 次基线校验零告警;注入坏块被逐槽位定位(论文2 引言动机实验) |
| DSA top-1 认证率 | **99.26%** | GLM-5.2,η=1e-3,21 层×103,516 行;逐层区间 90.0–100% |
| DSA 严格集合认证率 | **0.05%** | top-2048 集合口径过强(负结果);认证内翻转恒 0 |
| V4 条目二次量化 int8 残差 | **0.628%** | 43/43 层×92,791 行;与 GLM 生产 fp8 的 0.93% 同量级 |
| V4 带范数见证保守度 | **3.51×** | W/‖Δ‖,B=16,int8;逐层 2.73–3.86 |
| V4 页级 top-1 认证率 | **99.40%** | η=1e-3,21 个 C4 索引层;V4 选压缩页,与 DSA 的 token 级量纲不可直接比 |
| KDA 收缩因子 ā | **0.530–0.936** | Kimi-Linear-48B 代理,20/20 KDA 层全部 ā<1;半衰期 1.1–10.4 步;逐层标量近似口径 |
| KDA 近 1 通道占比 | **0.0–28.7%** | P(a_t≥0.999) 逐层区间;这些通道上界退化为线性累积 ⇒ 界须逐通道给,属紧度限制而非 soundness 问题 |
| 哨兵单块检出概率 | **0.881** | 有效轮数 n≈17(注入 rank 注入后的校验轮数,非全 rank 总和),每轮抽 128/1024;超几何闭式 |
| 哨兵检出 2/4 的概率 | **0.072** | '至多检出 2 个'的概率;实测 2/4 属正常涨落,是抽样受限而非能力上限 |
| 哨兵检出延迟旋钮 | **148 / 74 / 37 / 19 轮** | B=1、δ=1e-2 时 r=32/64/128/256 所需轮数;检出延迟与抽样预算近似成反比 |
| V4 池化粒度 | **4 与 128 并存(21/20 层)** | 同一模型内两种 hc 池化块大小;1/block 差 32 倍,跨层比较必须用归一化量 |
| V4 池化集中度(block4) | **0.1551** | top1_norm 中位(0=完全均摊,1=完全主导);有效 token 3.65/4;经验观测,不称证书 |
| 选择段契约 b_S | **0.00203** | Lemma S2:(a_S,b_S)=(1,½(m_out+m_in));η=1e-3,V4 C4 索引器 21 层中位;选择段**加性不放大**上游误差 |
| V4 三路自检违反 | **0** | 见证违约 + 认证内翻转 + 池化不变量违反合计;存储侧 sound / 选择侧条件认证 / 池化侧无认证 |
| 自适应采样覆盖地板兜底 | **0** | 高负载阶段触发次数;0 表示 every 一直在安全区。覆盖两阶段均 43 层未塌 —— 地板存在的意义是覆盖塌陷没有免费警报,不是它常被触发 |
| 并发下探针开销(EVERY=64) | **吞吐 -3.9% / TTFT P99 +10.5%** | 并发 1/8/32/64,每档 3 次重复取中位;**均在逐档噪声以内** —— 只能给上界不能给点估计;关图口径 |
| 并发下探针开销(EVERY=16) | **吞吐 -19.6% / TTFT P99 +140.8%** | **双双超出噪声,是可主张的真实退化**;故常开生产档位取 EVERY=64 不取 16 |
| 图重放期累加(graph-safe) | **Δ元素 43176 / Δn_calls 168** | **带图运行**(未加 disable-cuda-graph)。CUDA graph 重放不执行 Python,故 n_calls 只涨 prefill 的 168 次,而设备累加器涨满 43008 次 decode 重放 + prefill;两者与预期分毫不差,静置后不再变化。**判据是累加器在重放期继续增长,不是带图下的开销** —— 开销数字仍是关图口径 |
| 带图全速探针开销(EVERY=1) | **吞吐 -52.8% / TTFT P99 +138.6%** | **带图口径**,单卡 Qwen2.5-7B + gqa-kv,并发 1/8/32 各 3 次重复取中位。EVERY=1 是唯一被验证过在重放期有完整覆盖的档位,故这是带图成本的**上界**。**与论文关图数字栈不同,不可相减** |
| 带图下 EVERY=64 的重放增量 | **0** | 跑完整轮负载后累加器增量为 0,判定 NO_REPLAY_COVERAGE —— **采样门是 capture 时求值一次的 Python 分支**,开图后 EVERY 的语义从'每 N 次采一次'变成'这张图采不采'。该档层覆盖仍显示 28/28(起服期陈旧样本),验收门原本会放行,故新增设备侧执行计数与 --expect-replay 才能拦住 |
| 带图最省档探针开销(1 层 × 单量化档) | **吞吐 -2.26% / TTFT P99 +1.32%** | **带图口径**,capture 时按层号选择性插桩(确定性旋钮),**未用融合算子**。重放累加与层覆盖对账均通过 —— 不是'探针不在图里'的免费。仍超出 c=32 档的噪声下界,故仅靠减插桩点**压不进噪声**;把 kernel 数压到 1 之后的同覆盖档见'带图常开档' |
| 带图常开档(融合算子,1/28 层) | **吞吐 -0.94% / TTFT P99 -0.14%** | **三条判据同时满足**:重放累加为真、退化在逐档噪声下界内、层覆盖与声明一致。**成立的是'1 层常开'不是'全层常开'** —— 同为融合算子,全 28 层仍要 -10.16% |
| 融合算子在全层覆盖下的收益 | **5.2×** | 同为 28/28 层带图运行:torch 版 -52.81%,融合版 -10.16%。**kernel 数是成本的驱动量**,这是把它从几十压到 1 的直接收益 |
| 带图插桩成本的标度 | **1.72×** | 同为 2 层,量化档从 2 减到 1 的吞吐成本之比 —— 另:同为单量化档,层数 2->1 的比为 1.97×。**成本 ≈ 层数 × 每次执行的 kernel 数,两个旋钮各自近似线性且可乘**;故开图后的成本驱动量是 kernel 数,不是采样率 |
| 融合见证算子的数值等价 | **9.3e-07** | 融合 Triton 版与 torch 版在四组形状上的最大相对误差(mean/min/max);**viol 计数与样本数完全相等** —— viol 是 soundness 自检,不容许近似。舍入取 away-from-zero 而非 torch 的银行家舍入,仅在精确 .5 处不同(测度为零) |
| 融合见证算子单调用耗时 | **12.3 us** | 对 torch 单量化档 24.2×、双量化档 44.6×;**第一版用 grid=(1,) 想避开原子操作,反而 346us 比 torch 还慢** —— 拿并行度换原子操作是亏本买卖,改按行分块 + 三维分带归约才拿到这个数 |
| 全链逐层注意力 TV 界(typical) | **0.992** | 存储(窗口+压缩页均见证)+ 经验桥换算的选择误差,均 attn_dist:tv;c4 层(21 层)中 11 层非空洞。**组合档位 empirical**(选择段经经验桥,最弱段决定);c128 层页侧未见证分开报 |
| 经验桥系数 ρ | **0.87** | 注意力口径遗漏质量 / 选择器口径遗漏质量,截断集上实测;中位 0.87 接近 1(尾部 2.18,折算取 ρ_max 保守)。**选择器质量是注意力质量的可用代理** —— 外推到 η 扰动换手集是均质性假设 |
| 选择误差项(注意力单位) | **0.00444** | ρ_max × b_S;empirical 档。**不是瓶颈** —— 主导是存储见证(centry max 1.040 比 swa 还大) |
| C4 截断遗漏质量 | **0.379** | top-512 截断遗漏的假想全压缩注意力质量,中位(最大 0.686);**架构设计量而非误差** —— 稀疏选择是 V4 的模型语义,不计入误差链;本负载 decode 采样口径 |
| 请求级账本:保留优先工作点 | **保留 80.1% / 每步 TV 界 1.54** | **离线重放原型,非在线闭环**;条目级确定性证书(W ≤ 0.70)+ 假想局部回退;n=12,386,472 条目,c4 21 层。保留率=压缩收益保留,不称 certified coverage(其每步界空洞须一并说)。非空洞界与 ≥80% 保留不能同时成立(int8 代理见证无重尾)。Σδ=0;Lean: ledger_sound/telescope_sum/two_layer_budget_le/unknown_length_price |
| 请求级账本:界优先工作点 | **TV ≤ 0.86 / 保留 29.3%** | W ≤ 0.50 的条目走压缩,其余逐条目精确读取(局部回退)。构造性:被读条目全 ≤ 阈值 ⟹ 每步 TV 界成立;探针为采样审计口径,生产化=逐读取检查(gating 补丁) |
| 在线影子账本:逐读取事件数 | **198,163,408** | 25 条串行请求,逐请求账本在线建立;每 decode 步 × c4 层 × 全部被选中页条目(非抽样)。**只读影子**:超阈条目照常被使用,回退为假想(would_*);真实回退与概率预算消费是 A9 终局 |
| 在线影子账本:逐请求保留率中位 | **0.334** | W_thr=0.50;min 0.321 / max 0.701;假想页入中位 5373 MiB/请求 —— 这就是把回退做实的代价预告,与 p80 离线曲线一致 |
| 贪心解码重启稳定性地板 | **6/8** | off vs off2(零干预、两次独立起服):n=8 中即有分歧 —— **一切输出级对比的噪声地板**。p83 的'策略保护输出'对比缺此地板,已由本口径修正:干预效应真实(all 臂重度分歧),但 policy/dither 与基线的差异在 n=8 下处于地板附近,不可分辨;不称 protective,称 consistent with |
| 写策略接受率(精度粗化) | **48.0%** | **掩码条目占比/策略接受率 —— 非物理压缩收益**(字节数不变,是值域粗化;物理收益需打包另案)。窗口 nope 侧 |
| 抽签前授权:预授权率与保守代价 | **7.8% / 6.2×** | 授权只看先验半径 u_e(均值项+尾项),**不看随机实现**;保守代价 = policy 事后接受率 / dither 预授权率 —— 先验尾项的价格,如实报 |
| 预授权违约审计 | **0/147,208** | 事件 E_i='授权条目实现 W 超预授半径' 的运行时验证:全部预授条目实测违约计数;δ_i 自此**绑定真实概率事件**(五审 P0-1 修复),假设 R3′ 逐事件计数留痕 |
| 同请求闭环 Σδ | **0.0099997** | 读侧逐请求账本与写侧真实策略**同一次运行**;逐请求快照差分出写侧逐请求账目(每请求一行:读侧检查数/写侧压缩与回退增量/δ 增量/违约);Σδ < δ_req=0.01(望远镜),每份 δ_i 绑定事件 |
| 全 43 层存储项结构落账 | **dense 2 / c4 21 / c128 20** | 41 个有页层的页侧全部见证(写后读回口径);L0/L1 为 ratio=0 稠密层,无压缩页是架构事实非测量省略。c128 无索引器稠密读,无选择项 |
| c128 层逐层 TV 界(worst) | **0.976** | 中位;c4 侧为 1.339(含经验桥选择项)。c128 页见证 max 0.523 比 c4(1.045)干净约一倍 —— 粗压缩层反而更好认证;43 层中 18 层 worst 界 < 1 |
| 真实生命周期请求身份:user 请求独立预算 max Σδ | **0.009982** | 七审 F2 重做:边界 = 写路径 extend 转换(先于任何写入,无一批偏移),UID 永不复用;8 个 user 请求账户(warmup/boot 1 个分开计)全部独立 ≤ δ_req=0.01;单元测试 R5 覆盖复用+更长初始长度等漏检情形。串行 batch=1 口径 |
| SR 随机流:跨请求零复用 | **(nonce,UID,layer,k)** | 七审关键修复:种子四元组含永不复用的请求 UID —— 不同请求不共享随机数,条件随机性对自适应流量成立(Lean 超鞅前提可实例化);四元组跨 rank 一致,8-rank 账目逐字段一致(耦合保持) |
| 全局 e-process(跨请求不重置) | **峰值 -0.82 vs 阈值 4.61** | 持续全服务漂移哨兵:120,661 因子跨全部请求单调累积(终值 -76819.0),越阈 0 次;log_M_max 仅含更新后值(不以初始化 0 冒充峰值)。κ=0.5 预注册,δ_e=1% |
| owner 版本化:复用压力下闭环 | **复用 11,763 次 / 读未写 0 次** | G2:认证写逐槽位记录写入者 UID,复用 = 写时 owner 变更(压缩池无独立 free 路径,复用必先重写,flags 恒新鲜 —— 设计即约束,无需 allocator hook);不同 prompt 各账户 1,000+ 次复用下 foreign=0,自有闭环精确。口径:串行页回收 + 前缀共享;**请求中途驱逐**负载未覆盖 |
| foreign 读归属(前缀共享) | **1,063,892(重复 prompt 占 974,386)** | 读到别的请求写入的条目按 owner 分账 —— 重复 prompt 94% 读命中前缀缓存(owner=首写请求),不再误记入本请求闭环;写侧 δ 由 owner 请求承担,读侧检查确定性 δ=0,跨请求共享不破坏预算语义 |
| 并发逐行身份:8 路并发独立预算 | **max Σδ = 0.009984** | G1:批内多请求混行下逐条目 UID 归属(decode 计划 [bs,16] 批行对齐 / prefill 逐 query token 前缀和分段);8 个 user 账户各自独立 ≤ δ_req=0.01;每账户独立 (nonce,uid,layer,k) 随机流;8-rank 耦合逐字段一致 |
| 并发归属守卫:foreign 读 | **0/8,228,864** | owner 分账做成归属正确性的**运行时守卫**:首验 foreign 3.4% 触发,定位出 c4/c128 两池共用键空间的 owner 污染(串行同刻单 uid 掩盖,并发暴露 —— 最早分配者低号槽位与 c128 数值区重叠);键按压缩比分离后8,228,864 次读 foreign=0、读未写=0 |
| 硬化验收门(machine-decidable) | **12/12 PASS** | 八/九审收口:**全零硬判据**(foreign/unwritten/violations/归属回退/身份 fail-closed 拦截/e-process 有数且未越阈/独立预算/账户数/8 份 rank 快照齐且耦合),判读脚本退出码承载 —— CI/发布包可直接依赖;p99 为 fail-closed 身份+违约恢复+generation 计数全部在线后的端到端复验(拦截 0/恢复 0 = 硬化机制在正常流量下惰性,只在异常时接管) |
| 低比特案例:packed INT4 独立池字节(GPU 实测) | **36.5→22.5 MiB** | **独立池格式收益,非服务进程总显存**(FP8 原池仍是 FlashMLA 真实输入);64Ki token,memory_allocated 差分 −38.4% |
| 低比特案例:serving 内 pack↔unpack 自洽 | **12,960 检查 / unwritten 0 / rel 10.2%** | 真实 360B/token packed 池在 serving 内三跑稳定;工具证伪两条设计假设:①槽位渐进演化(只改 pack 时机误差 18×)②python 读路径非真实消费者(FlashMLA kernel 内直吃 FP8,三探 packedread=null)。**不声称**:INT4 已被 attention 消费/HBM 已降/吞吐/质量;rel_max≈4.9 归因演化期重读(近零范数假设被数据否决) |
| 四证书裁决:非空洞 43 层 | **C1 0 / C2 43 / C3 6 / C4 25** | 认证对象=掩 2 位 SR(θ/Δ 由捕获字节离线重建);中心化 tanh(osc/4) 使全部43 层非空洞(现行形 0/43);soundness 自检:全部半径零被击穿(顶部夹取的确定性平移与零权重分支 log(0) 两个 bug 均被自检抓出后修复) |
| 四证书裁决:tightness(半径/实测中位) | **C1 15.8× / C2 5.2× / C3 2.9× / C4 2.1×** | 64 次 SR 实现为地面真值;现行形 15.8× 松 → 中心化/精确 MGF 2.0×(8 倍收紧)。**门1(≥30/43)过;门2(retention@0.2 ≥1.5×)败** —— 全族 retention=0,τ=0.2 在掩 2 位粒度不可达;按预注册规则 E4 重 kernel 跳过。部署分野:写时预授权 x 在手 θ 免费(精确 MGF 零元数据);读时需 θ ≈+39% 存储 |
| e-process 累计收紧 | **390×** | T=33,407 事件,**双侧**累计对象(公平口径,各付 δ/2):595σ vs 望远镜求和 231,866σ。**失败事件不同**:望远镜界'任一逐事件半径被突破'(条目授权用),e-process 界有符号累计和 —— 后者是累计误差的可形式化路线,**尚未接入逐条目授权账本**(F4) |
| 认证写路径开销(未融合) | **1.20×** | 逐请求墙钟中位 off 26.1s → dither 31.3s;关图 + python 路径口径 —— 融合 kernel 被 E1 门2 按预注册规则挡下,此为**未优化实现的诚实成本**;物理字节不变(精度粗化非物理压缩) |
| KDA 对数收缩 E[log a] | **-0.5912** | 正确判据(乘积口径)的逐层中位;均值口径 log(ā) 为 -0.2282 —— **均值把收缩算弱了 2.6×**;全 20 层 E[log a] 均为负 |
| KDA 误差半衰期 | **1.17 步** | 对数和判据;均值口径会报成 3.04 步。口径:E[log a] 是 (通道,时刻) 聚合,**不是逐通道 Σlog a_t** —— 近 1 通道占比中位 0.0578,其上不收缩 |
| 桥接后逐层注意力 TV 界(typical) | **0.647** | **窗口存储项**,精确形 ½(e^{2ε}−1),40/43 层非空洞;ε = scale·‖q‖·W(打分桥 Cauchy–Schwarz × softmax 桥 e-form 实例化),q 范数与 W 均为采样分布统计量。**压缩页条目量化项未见证;选择段 b_S 在 selector_dist:tv,不相加**。每段 proof 指向已证 Lean 定理 |
| 桥接后逐层注意力 TV 界(worst) | **0.786** | 全 rank 逐层最大值口径,29/43 层非空洞;Cauchy–Schwarz 取 Δk 与 q 对齐的最坏情形 —— sound 但悲观,这是证书的代价 |
| 桥接系数 scale·max‖q‖ | **1.0034** | q 过 rmsnorm 后 scale·‖q‖ ≈ 1,打分桥几乎不放大;界的主导项是绝对见证 W(全层最大 0.700)。p75 实采,43 层 × 8 rank |
| 请求级 TV 聚合预算 | **36.3** | 43 层求和(worst 口径),>> 1 —— **空洞,如实报**。跨层求和是聚合预算而非端到端输出距离(各层是不同的分布);被类型检查废掉的 0.982 连界都不是,不能拿本数字与它比较 |
| 平台覆盖:过验收门的探针行 | **8 行 / 6 模型** | 架构族 GQA、KDA+MLA 混合、MLA(无稀疏索引)、MLA+DSA、MLA+压缩条目+稀疏索引;适配器 6/注入点 11;收录门槛=通过覆盖->量纲->soundness 验收门 |
| GLM latent 二次量化 int8 残差 | **0.551%** | 统一框架,78/78 层,采样 EVERY=16;保守度中位 2.95× |
| 探针开销(最不利档) | **+113.24%** | 单请求串行无并发 = 探针开销的最不利放大口径;关图状态下的**相对增量**, 不代表带图生产的端到端开销;每档 3 次重复取中位 |
| matched 五臂:radix 掩蔽 | **0.800 vs 0.050** | packed 臂 radix on/off;base 两侧均 1.000 —— 0.800 是缓存命中口径非质量,confounding 由同 launcher 五臂排除 |
| freshness 判别:pack-once vs every-read | **0.050 vs 1.000** | 20 篇 niah,两臂仅差 PACK_EVERY_READ 且读写双路径均有执行计数;everyread 恢复 ⇒ 代际一致性是主因(诊断配置,不作唯一归因) |
| 生产 pack 路径本征误差 | **0.0245** | 真实 c4 条目 4,998 行/21 层,纯核/池路径/覆盖写三档同值,max 0.0304,rope 段恰 0 |
| 代际失效因果确认(第二次) | **INVALIDATION_CONFIRMED** | 六条机器判据全过:inval_on acc=1.000 / inval_off acc=0.200(并发 4 口径,两臂仅差失效开关,n_invalidate 双向证据);不覆盖 abort/radix-on/HiSparse/MTP/PD |
| 解码侧影子:修复后 c4 保真度 | **0.0246** | 内核即将读到的 packed vs 此刻 FP8 源(重填前,1/32 采样,n=33,872);本征 0.0245,对照臂 0.8816(36×) —— centry 站点同指标对本失效零区分度(双写者构造性跨代比较) |
| 阶段 A 池装配(规范寻址) | **acc 1.000** | c128_kv_pool→SwappedC128Pool,20 层 remap(rank 快照 installed=1),与 noswap 臂 acc/解码侧 rel 一致 —— 结构性换池零质量代价 |
| stage-B HBM 实测差分 | **2,988 MiB** | FP8 全量 c128 池→窗口环(ring_pages=257)+ packed 常驻,8 卡 aggregate 总差;核算 3,040 MiB,aggregate 相差 1.7%(逐卡 [566,346×7] 非均匀,有误差抵消,不得称逐卡吻合);c128 仅 ~0.3% 总 HBM(c4 是 32× 大头),机制验证口径 |
| stage-B bring-up 质量(负结果) | **0.500 vs 1.000** | 6 篇 niah miss 3;HTTP n_error=0(产物 snapshot 空,**不能**声明内部 err/orphan/8rank installed —— 证据链洞,GPT review);后由 p112d 定谳为 decode 越界读环(寻址缺陷),环链是否无损未证明 |
| 参考消费者:packed 服务内消费 | **26,440 行** | 三分类恒等式 31,560+26,440+0+240,420=298,420(rank0 聚合,逐调用去重槽);oracle/refcons acc 1.000/1.000,tol=0;SMOKE 6 docs,环 129 页,覆盖随驱逐失效语义;不称无损 |
| c128 单层 staging HBM 净差 | **316 MiB/GPU** | 实测中位 vs 字节净差 354.6 MiB(freed 639.2 − ring+packed 284.6);全部 untyped_storage 直读;c128 仅 ~0.3% 总 HBM,机制口径 |
| c4 INT6 替换 HBM 净差 | **2,108 MiB/GPU** | native vs c4-staging1(formal 九门含 abort 扰动),字节净差 2,200 MiB(freed 14.24 GiB=20/21 层,张量直读);双臂 acc 1.000/1.000;SMOKE 探针配置(双图关),c128 保持原生 —— 单独口径,不与 c128/图模式数字混算 |
| packed kernel 包装器调用(双图开启) | **5,576 次** | **capture/eager 路径计数口径**(replay 执行录制内核,不回 host 计数;replay 图含 packed 分支由'capture 时所走分支'推得):c128 INT4 2,720 + c4 INT6 2,856;双臂 acc 1.000/1.000,negflag/uncovered=0,8 rank;fallback 272 已归因无压缩缓存调用;SMOKE 单 seed 受限配置 |
| radix×stage-B 质量退化(复现) | **0.667 vs 1.000** | q6c 与 q6k(全链口径)两轮独立复现;radix-off 装配门显式拒绝该组合;吞吐坍缩为日志观察无独立 bench 产物,不作性能结论 |
| PD 分离:native 通过 / stage-B 布局缺口 | **1.000 / 0.000** | 单机双进程(prefill TP4+decode TP4+mini-lb,mooncake);native 针跨进程答对=KV 真传输;stage-B 0/6 且 n_error=0 —— 传输完成、内核被调、服务不崩但语义错:布局契约未被传输层承载(runtime contract 必要性的实证);TP4×2 口径 |
| 重复运行答案稳定性 | **acc 1.000/1.000** | 同配置串行重复对(带路由),逐题文本 5/6 不同(上游非确定推理;dsv4 不在 deterministic 白名单)——只主张答案稳定性,不主张逐字确定性 |
| staging1 服务时延比值 | **吞吐×0.884 成本×1.131** | bench_serving random seed42 N=32;探针配置非生产;staging1 为逐调用全量物化未优化实现 = 上界开销;仅臂间可比,**不构成**token 成本降低声明 |
| fused 解包核服务比值(含脚手架) | **吞吐×0.238 成本×4.201** | fusedk 臂含逐调用 ensure_fresh+Python 逐槽审计(非共模诊断脚手架)—— 比值是上界**不是纯核开销**;功能面独立成立(597,981 槽审计 stale=0,质量锚 1.000) |
| 图模式 12-doc 探针(负结果) | **0.750** | 无 MTP、12 docs、WORKERS=4:doc 稳定漏针(15@0.5/16@0.85/18@0.5);MTP 双 seed 同树 0.667/0.500(漏针集与之高度重叠)—— 6-doc SMOKE 口径掩盖的系统性残差,cudagraph 图内消费声明由此限定为 6-doc;十四轮判别链定谳:预填压缩上下文读为必要通道且依赖 c4 环驻留(见判别链条目) |
| 判别:零驱逐环下 W4 并发全对 | **1.000** | ring1025(n_evict=0)12-doc W4 = 1.000;同轮对照 ring513 = 0.833(n_evict=256)—— 腐坏驱逐介导;串行 W1(q6o)= 1.000 但 n_evict=0,'串行干净'是混杂变量(未曾行使驱逐),真不变量 = 在飞请求页被驱逐 ⇔ 腐坏 |
| 判别:分池双解离定位 c4 | **1.000 / 0.667** | c128 独驱(256 次)acc=1.000 全对 vs c4 独驱(353 次)acc=0.667 —— c4 池驱逐特异;c128 驱逐无害此后又两轮复现(q6p6/q6p7 均 1.000) |
| 判别:eager 写链取证无罪 | **16,942,020** | 烘定-存储逐行对账 8,264,400(c128)+8,677,620(c4)行,mismatch=0,形状漂移=0 —— build→forward 间环槽从未易主;图内 finalize 撞已释放页计数亦为 0;fallback 调用全为无压缩缓存口径(extra_k_cache=None),与 c 池无关 |
| 判别:预填 extra 必要性消融(收口轮) | **0.000 / 0.667** | extra_topk_length 清零(执行自证 5,125 次)acc=0.000 全灭 vs 对照 0.667(c4 剂量带内)—— 预填压缩上下文读是任务必要通道;叠加坐标域错乱(kv-cache 坐标索引环缓冲,c4 越界 66 倍)与 14 轮驱逐剂量不变量,机制闭环:该通道数据依赖 c4 环驻留。修法=预填 extra 物化适配层(roadmap);短期口径=环≥活工作集(唯一已证干净配置) |
| MTP 零驱逐口径重测(改判 PASS) | **1.000/1.000 与 1.000/1.000** | EAGLE 1步,12-doc W4,双环 1025(quality_caliber 零驱逐自证):seed42/43 双臂全对 —— 每轮为同轮双臂受控对照,无跨轮归因需求;历史残差(q6i3 5/6、q6m 14/24)由 14 轮判别链归因为 c4 驱逐机制混杂,非 MTP 特异。矩阵 mtp 改判 PASS |
| Q10 终表数据轮(零驱逐口径,未优化物化路径) | **26.8 vs 173.4 tok/s** | bench(32 请求,2048/128):output 吞吐 ×0.155,TTFT mean 53.7s vs 2.9s;HBM ~127.7 vs ~122.6 GiB/GPU(大环口径反超,节省被环撑大抹掉)—— 真实代价口径,收益主张不作。**质量锚 0.917(11/12):零驱逐反例** —— bench 前置使逻辑槽复用,环页仍驻留即喂前任请求行(needle_0 输出他请求暗号),'零驱逐⇒干净'必要非充分;引用本表必须带此口径 |
| 判别:读侧物化修正无效(伤害通道重开) | **0.750 / 0.750** | 强制 sparse 预填分支 + stageb-r:n_translate_read=8,023,680 执行自证、orphan=0(非驻留全命中 packed),c4 驱逐 355 次 —— acc=0.750 与对照 0.750 同带同族。'预填 extra 错读是驱逐伤害媒介'证伪;与消融合并:通道必要、预填寻址非媒介 —— 修错了相位(后由 q11t2 定谳:伤害消费者= decode 侧 extra 读,见下两条) |
| 判别:持久态跨臂全同(v6 内容对拍) | **c4 中位 0.0026(底噪 0.0021);SWA 中位 0.0016(底噪 0.0010)** | L1+有序多重集度量,同配置重复对先定底噪再判读:c4(q11q,96,095 配对元素)>10% 键 1.9% vs 底噪 1.3%;SWA(q11s,181,374 元素)>10% 0.0% vs 底噪 0.0% —— 腐坏臂(0.500)与健康臂(1.000)的 c4 与 SWA 写入内容均在底噪带内,持久态全同;SWA 行是预填残差流的函数,故预填计算两臂等价,伤害只能在 decode 瞬态。前代哈希差判读(q11l)以坏度量作废 |
| 判别:decode extra 读侧未翻译定谳(干预确认,46 轮收官) | **1.000 vs 0.500** | 机制:decode extra 读以逻辑槽直索物理坐标的后备存储(读侧翻译执行数 n_translate_read=0;请求逻辑条目上界 8,500 > ring129 物理容量 8,256)—— 零驱逐+单请求时物理=逻辑恒等映射侥幸成立,驱逐回收/多请求共环即读到他条目内容且 written/代际审计不报警(q10a 零驱逐反例同机制)。干预:非驻留读折 -1(1,271 调用)= **诚实截断被逐条目**(~26% extra 行被弃,微缩仍全对;后续判别定谳:重映射对请求 id ≈恒等,疗效即截断本身,且时机敏感 —— 驱逐落进预填窗口时截断亦致 0.500),W1 ring129(驱逐 20)0.500→1.000,对照臂翻译计数确认为空,驱逐/调用数两臂全同。**范围口径与开放问题**:W1 微缩干预有效性确认(驱逐晚于预填时);W4 规模剂量分层四对+方差基线判定 —— 剂量随机、同剂量两臂共曲线(截断与腐坏等价伤害);九轮穷尽仪器化(选择流含底噪/服务内容/档完整性/键控/topk)全部跨臂等价后质量分裂依旧,**伤害微观机制最后一公里开放**;生产可证语义 = 零驱逐 + 请求隔离(三轮 W4 零驱逐全对) |
| 双池 PPL 配对(teacher-forcing) | **1.3496 vs 1.3495** | tp4 同口径双臂(此前 base 沿用 tp8 的 p108,跨 tp logprob 有真实差,已重制):natural+code 24 篇 × 4k 前缀,98,568 token 逐 token logprob;packed(INT4/INT6 消费)ΔPPL -0.00% —— PPL 口径中性。flagship dual_pool_ppl 由 PENDING 转有数;激活双向门口径:packed 臂 消费计数为正,base 臂以 manifest 证明 packed 开关/适配器缺席 |
